apiVersion: v1
kind: Service
metadata:
  name: {{ include "restaurant-reservation.fullname" . }}
  labels:
    {{- include "restaurant-reservation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "restaurant-reservation.selectorLabels" . | nindent 4 }}

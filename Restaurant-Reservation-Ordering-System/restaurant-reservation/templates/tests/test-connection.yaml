apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "restaurant-reservation.fullname" . }}-test-connection"
  labels:
    {{- include "restaurant-reservation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "restaurant-reservation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
